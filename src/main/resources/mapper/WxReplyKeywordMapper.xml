<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minstone.mobile.mp.wechat.reply.dao.WxReplyKeywordDao">

  <resultMap id="BaseResultMap" type="com.minstone.mobile.mp.wechat.reply.domain.WxReplyKeyword">
    <id column="KEYWORD_CODE" jdbcType="VARCHAR" property="keywordCode" />
    <result column="RULE_CODE" jdbcType="VARCHAR" property="ruleCode" />
    <result column="KEYWORD" jdbcType="VARCHAR" property="keyword" />
    <result column="MATCH_FLAG" jdbcType="INTEGER" property="matchFlag" />
    <result column="DEL_FLAG" jdbcType="INTEGER" property="delFlag" />
  </resultMap>

  <sql id="Base_Column_List">
    KEYWORD_CODE, RULE_CODE, KEYWORD, MATCH_FLAG, DEL_FLAG
  </sql>

  <insert id="insertSelective" parameterType="com.minstone.mobile.mp.wechat.reply.domain.WxReplyKeyword">
    insert into MP_YY_REP_KEYWORD
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="keywordCode != null">
        KEYWORD_CODE,
      </if>
      <if test="ruleCode != null">
        RULE_CODE,
      </if>
      <if test="keyword != null">
        KEYWORD,
      </if>
      <if test="matchFlag != null">
        MATCH_FLAG,
      </if>
      <if test="delFlag != null">
        DEL_FLAG,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="keywordCode != null">
        #{keywordCode,jdbcType=VARCHAR},
      </if>
      <if test="ruleCode != null">
        #{ruleCode,jdbcType=VARCHAR},
      </if>
      <if test="keyword != null">
        #{keyword,jdbcType=VARCHAR},
      </if>
      <if test="matchFlag != null">
        #{matchFlag,jdbcType=INTEGER},
      </if>
      <if test="delFlag != null">
        #{delFlag,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>

  <insert id="insertBatch" parameterType="com.minstone.mobile.mp.wechat.reply.domain.WxReplyKeyword">
    insert into MP_YY_REP_KEYWORD (<include refid="Base_Column_List"/>)
    values
    <foreach collection="list" item="item" index="index" separator=",">
      (#{item.keywordCode}, #{item.ruleCode},  #{item.keyword}, #{item.matchFlag}, #{item.delFlag})
    </foreach>
  </insert>

  <update id="deleteByPrimaryKey" parameterType="java.lang.String">
    update MP_YY_REP_KEYWORD
    set DEL_FLAG = 1
    where KEYWORD_CODE = #{keywordCode,jdbcType=VARCHAR}
  </update>

  <update id="deleteByRuleCode" >
    update MP_YY_REP_KEYWORD
    set DEL_FLAG = 1
    where RULE_CODE = #{ruleCode,jdbcType=VARCHAR}
  </update>

  <delete id="forceDeleteByPrimaryKey" parameterType="java.lang.String">
    delete from MP_YY_REP_KEYWORD
    where KEYWORD_CODE = #{keywordCode,jdbcType=VARCHAR}
  </delete>

  <delete id="forceDeleteByRuleCode" parameterType="java.lang.String">
    delete from MP_YY_REP_KEYWORD
    where RULE_CODE = #{ruleCode,jdbcType=VARCHAR}
  </delete>

  <delete id="forceDeleteByRuleCodeBatch" parameterType="java.lang.String">
    delete from  MP_YY_REP_KEYWORD
    where RULE_CODE IN
    <foreach collection="array" item="item" index="index" open="(" close=")" separator=",">
      #{item}
    </foreach>
  </delete>

  <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from MP_YY_REP_KEYWORD
    where KEYWORD_CODE = #{keywordCode,jdbcType=VARCHAR}
    and
    DEL_FLAG = 0
  </select>

  <select id="selectByRuleCode" parameterType="java.lang.String" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from MP_YY_REP_KEYWORD
    where RULE_CODE = #{ruleCode,jdbcType=VARCHAR}
    and
    1=1
  </select>

  <update id="updateByPrimaryKeySelective" parameterType="com.minstone.mobile.mp.wechat.reply.domain.WxReplyKeyword">
    update MP_YY_REP_KEYWORD
    <set>
      <if test="keyword != null">
        KEYWORD = #{keyword,jdbcType=VARCHAR},
      </if>
      <if test="matchFlag != null">
        MATCH_FLAG = #{matchFlag,jdbcType=INTEGER},
      </if>
      <if test="delFlag != null">
        DEL_FLAG = #{delFlag,jdbcType=INTEGER},
      </if>
    </set>
    where KEYWORD_CODE = #{keywordCode,jdbcType=VARCHAR}
  </update>

  <update id="updateBatch" parameterType="com.minstone.mobile.mp.wechat.reply.domain.WxReplyKeyword">
    UPDATE MP_YY_REP_KEYWORD
    <trim prefix="set" suffixOverrides=",">
      <trim prefix = "KEYWORD = case" suffix="end,">
        <foreach collection="list" item="i" index="index">
          <if test = "i.keyword != null">
            WHEN KEYWORD_CODE = #{i.keywordCode} then #{i.keyword,jdbcType=VARCHAR}
          </if>
        </foreach>
      </trim>

      <trim prefix = "MATCH_FLAG = case" suffix="end,">
        <foreach collection="list" item="i" index="index">
          <if test = "i.matchFlag != null">
            WHEN KEYWORD_CODE = #{i.keywordCode} then #{i.matchFlag,jdbcType=INTEGER}
          </if>
        </foreach>
      </trim>
    </trim>
    WHERE
    <foreach collection="list" separator="or" item="i" index="index">
      KEYWORD_CODE = #{i.keywordCode,jdbcType=VARCHAR}
    </foreach>
  </update>

</mapper>